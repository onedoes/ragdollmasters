//

// import { Bodies, Composite, Constraint } from "matter";

//

// const createRagdoll() {

// }
export class Ragdoll extends Screen {}
/*
export function Ragdolll() {
  const head = Bodies.circle(100, 100, 50, {
    label: "head",
    // chamfer: {
    // radius: [15, 15, 15, 15],
    // },
    render: {
      fillColor: 0xffbc42,
    },
  });
  const headContraint = Constraint.create({ bodyA: head });
  return Composite.create({ bodies: [head], constraints: [headContraint] });
  // const draw = useCallback(
  //   (g: Graphics) => {
  //     g.clear();
  //     g.lineStyle(props.lineWidth, props.color);
  //     g.drawRect(
  //       props.lineWidth,
  //       props.lineWidth,
  //       props.width - 2 * props.lineWidth,
  //       props.height - 2 * props.lineWidth
  //     );
  //   },
  //   [props]
  // );
  // return <Graphics draw={draw} />;
}
*/
